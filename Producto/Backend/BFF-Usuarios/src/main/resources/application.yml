server:
  port: ${SERVER_PORT:8082}

spring:
  application:
    name: bff-usuarios

ms:
  users:
    url: ${MS_USERS_URL:http://localhost:8090}

jwt:
  secret: ${JWT_SECRET:fitproject-secret-key-must-be-at-least-32-chars-long}
  expiration: ${JWT_EXPIRATION:86400000}
